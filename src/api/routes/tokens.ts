import { Router } from 'express';
const router = Router();

// TODO: Implement token routes
router.get('/', (req, res) => {
  res.json({ message: 'Token routes not yet implemented' });
});

export default router; 